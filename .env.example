# Modo demo: retorna mocks dos services instantaneamente, sem chamar o backend.
# Ative no Vercel (ou localmente) para publicar a versão demo sem depender da API.
VITE_DEMO_MODE=true

# URL da API (apenas em produção real, quando houver backend)
# VITE_API_BASE_URL=https://seu-backend.vercel.app